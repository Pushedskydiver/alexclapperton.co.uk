image: node:4.2.2

pages:
  stage: deploy
  cache:
    paths:
    - node_modules/
  script:
  - mkdir .public
  - cp -r * .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master
