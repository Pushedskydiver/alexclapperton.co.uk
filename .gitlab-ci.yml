image: node:4.2.2

pages:
  tags:
  - shell
  cache:
    paths:
    - node_modules/

  script:
  - mkdir dist
  - npm install
  - gulp default
  - mv dist public
  artifacts:
    paths:
    - public
  only:
  - master
