---
title: Articles
description: Posts about web development, accessibility and more.
layout: _layouts/page
sitemap:
    changefreq: monthly
    priority: 0.8
---

{% from "_partials/_macros/topic.njk" import topicLink %}

<main class="bg-slate-800 [padding-block:32px] pr-safe pl-safe md:[padding-block:48px]" id="main">
    <div class="container [margin-inline:auto] [padding-inline:16px]">
         <div class="[padding-block:24px]">
            <h2 class="text-white text-md leading-md fvs-md [margin-block-end:16px]">Explore articles by topic</h2>

            <ul class="flex flex-wrap gap-16" aria-label="Article topics">
                {% for item in topics %}
                    {{ topicLink(item.name, page.url, item.icon, item.slug) }}
                {% endfor %}
            </ul>
        </div>

        <ul class="[margin-block-start:24px] grid gap-24 sm:grid-cols-2 md:grid-cols-3 sm:[margin-block-start:40px]" aria-label="Articles">
        {% for item in articles %}
            {% include "_partials/components/article.njk" %}
        {% endfor %}
        </ul>
    </div>
</main>

{% include "_partials/components/push.njk" %}
